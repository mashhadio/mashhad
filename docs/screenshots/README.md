# Screenshots

Drop README images here:

- `home.png` — recorder home screen with a source selected
- `editor.png` — editor with zoom blocks on the timeline
- `zoom-demo.gif` — a few seconds of exported output showing the cursor-follow zoom
